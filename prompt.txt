buatkan saya file issue.md dengan tujuan untuk pembuatan planing sebagai berikut:

1. buatlah table users dengan kolom:
- id (primary key, auto increment)
- name (varchar, not null)
- email (varchar, not null, unique)
- password (varchar, not null, hash with bcrypt)
- created_at (timestamp, default current timestamp)
- updated_at (timestamp, default current timestamp)

2. buatlah endpoint untuk registrasi user:
- method post
- endpoint /api/user/register
- body (json): {"name": "", "email": "", "password": ""}
- response (json): 
  - success: {"message" : "Ok"}
  - failed: {"error": "username or email already exists"}

3. ikuti rules berikut untuk struktur folder dan format nama file di masing-masing folder:
- di dalam folder src:
  - buatkan dua folder yaitu:
    1. routes -> folder ini untuk menyimpan file routing dari elysiajs, format nama file misalnya user_routes.ts
    2. services -> folder ini untuk menyimpan logic bisnis, format nama file misalnya user_service.ts


jelaskan tahapan-tahapanya untuk implementasi di atas, buatlah secara jelas dan detail karena
nantinya yang mengimplementasikan adalah junior developer atau model AI yang lebih
murah.
